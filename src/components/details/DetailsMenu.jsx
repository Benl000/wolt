import React from 'react';

export const DetailsMenu = ({restaurant}) => {
        return restaurant ? (
            <section className="restaurant-menu">
                menu
            </section>) : null;
};
